from os import listdir
from os.path import isfile, join
from nltk.stem.snowball import EnglishStemmer
import csv
import sys
import codecs

with codecs.open('amt_input_defs_examples.csv', 'rU', encoding='utf-8') as inputDefs:
    for lines in inputDefs:
        line = lines.split(",")
        



with codecs.open('amt_input_defs_examples.csv', 'rU', encoding='utf-8') as inputDefs:
    for lines in inputDefs:
        line = lines.split(",")
        for i in range(10):
            if line[i+2] in hedge_dict:
                
            

2, 11

0, 9